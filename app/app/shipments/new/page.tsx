import ShipmentNew from "@/views/ShipmentNew";

export const metadata = {
  title: "Create Shipment | Swish Portal",
  description: "Create a new shipment for tracking",
};

export default function NewShipmentPage() {
  return <ShipmentNew />;
}
