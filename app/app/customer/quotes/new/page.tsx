import { QuoteRequest } from '@/views/QuoteRequest';

export const metadata = {
  title: 'Request Quote - Swish Portal',
  description: 'Request a shipping quote',
};

export default function QuoteRequestPage() {
  return <QuoteRequest />;
}
